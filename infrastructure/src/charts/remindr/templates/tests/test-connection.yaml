apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "remindr.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "remindr.name" . }}
    helm.sh/chart: {{ include "remindr.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "remindr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
