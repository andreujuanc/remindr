echo HOLA
azure account list
echo $ARM_SUBSCRIPTION_ID
cd /infrasource
rm key.rsa
rm key.rsa.pub
/root/.pulumi/bin/pulumi stack select andreujuanc/remindr/various
/root/.pulumi/bin/pulumi config set azure:environment public
/root/.pulumi/bin/pulumi config set password --secret $CLUSTER_PASSWORD
ssh-keygen -t rsa -f key.rsa
/root/.pulumi/bin/pulumi config set sshPublicKey < key.rsa.pub
/root/.pulumi/bin/pulumi up


